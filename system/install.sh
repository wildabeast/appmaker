if [ ! -d "$dataPath" ]
  then
    mkdir $dataPath
    mkdir $activePath
    mkdir $portsPath
    mkdir $tempPath
    mkdir $logsPath
    mkdir $backupPath
    mkdir $PANELPATH
    mkdir $sitesPath
fi
