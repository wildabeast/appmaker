module('Navigation')

test('Navigation', function() {
  
  ok(nudgepad.navigation, 'namespace exists')
  
})


test('run through a series of apps', function() {
  
  ok(nudgepad.navigation, 'namespace exists')

  /*  
  var current = history.length
  ok(current, 'history length exists and is ' + current)
  nudgepad.navigation.open('blog')
  equal(history.length, current + 1, 'Opening an app added to history state')
  equal(ParseQueryString().app, 'blog', 'blog got added to query string')
  nudgepad.navigation.open('home')
  equal(history.length, current + 2, 'Opening an app added to history state')
  nudgepad.navigation.open('pages')
  equal(history.length, current + 3, 'Opening an app added to history state')
  equal(ParseQueryString().app, 'pages', 'pages got added to query string')
  history.back()
  ok(nudgepad.apps.blog.isOpen(), 'blog is open')
  equal(ParseQueryString().app, 'blog', 'blog in query string')
  */
  
})


